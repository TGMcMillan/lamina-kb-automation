name: Daily Summary Test

on:
  workflow_dispatch:

jobs:
  test-message:
    runs-on: ubuntu-latest

    steps:
      - name: Show a test message
       run: |
  echo "Reading kb-source..."
  cat kb-source/notice-management-update.md
